## Code Quality Rules
- Always run `pnpm build` after making code changes to ensure the extension builds successfully
- Always run `pnpm lint` after making code changes  
- Verify the build completes without errors before considering tasks complete
- Remove unnecessary comments
- Modify appropriate unit tests after code generation

## Key Commands
- `pnpm run codegen` - Generate protobuf code
